# Rust / WASM build artifacts
crate/target/
crate/Cargo.lock
crate/pkg/

# Staged build output — regenerated by build.sh / CI, never committed
www/pkg/

# Spike (Phase 0 throwaway feasibility crate — not shipped)
spike/

# Node (playwright for local testing only)
node_modules/

# Editor / OS noise
.DS_Store
*.swp
.idea/
.vscode/
test.mjs
